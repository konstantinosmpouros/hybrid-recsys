services:
  app:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - ./artifacts:/app/artifacts:ro
      - ./data:/app/data:ro
    environment:
      - PYTHONPATH=/app/src
    restart: unless-stopped
